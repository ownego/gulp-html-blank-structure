(function(window, $){
    var $doc = $(document);

    var config = {
    }

    // Common functions
    var oe = {
    };

    // Make it global
    window.oe = oe;
})(window, jQuery);

$(function() {


});
